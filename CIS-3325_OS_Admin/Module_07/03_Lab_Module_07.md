# Lab Activity: Module 07 - Archiving
## Course: CIS-3325_OS_Admin (CompTIA Linux+ (XK0-005))

---

**Objective:** Use `find` to locate a specific log file, then use `grep` to extract all lines containing the word "ERROR".
**Deliverable:** Screenshot of the `grep` command output showing the filtered error lines.
