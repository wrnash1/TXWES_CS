**Video Script 9.1: APT vs. YUM/DNF**
*Audio:* "Ubuntu uses `apt` to install `.deb` packages. RedHat/CentOS use `yum` or `dnf` to install `.rpm` packages. Package managers are brilliant because they resolve dependencies automatically. If you install Apache, `apt` automatically downloads the required libraries."
