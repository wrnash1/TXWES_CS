**Video Script 12.1: Cron and Crontab**
*Audio:* "To automate a script to run every Friday at midnight, we use `cron`. The `crontab` file uses five fields: Minute, Hour, Day of Month, Month, Day of Week. `0 0 * * 5 /opt/backup.sh` runs the script exactly at midnight on Friday."
