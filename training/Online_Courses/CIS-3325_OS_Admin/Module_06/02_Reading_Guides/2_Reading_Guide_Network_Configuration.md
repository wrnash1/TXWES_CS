### 2. Reading Guide: Network Configuration
**High-Yield Concepts:**
*   `/etc/hosts`: Used for local DNS resolution before checking a DNS server.
*   `/etc/resolv.conf`: Contains the IP addresses of the DNS servers the Linux machine uses.
