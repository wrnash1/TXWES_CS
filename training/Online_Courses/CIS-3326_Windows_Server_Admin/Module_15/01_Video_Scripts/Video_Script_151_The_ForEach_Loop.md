**Video Script 15.1: The ForEach Loop**
*Audio:* "If HR sends you an Excel spreadsheet of 100 new hires, you do not create them manually in ADUC. You write a PowerShell script. We import a CSV file, pipe it into a `ForEach` loop, and use `New-ADUser` to automate the creation of all 100 accounts in three seconds."
