**Video Script 12.1: First Hop Redundancy Protocols (FHRP)**
*Audio:* "If the default gateway router dies, the network dies. FHRPs like VRRP and HSRP allow multiple physical routers to share a single virtual IP address. If the primary router fails, the secondary instantly takes over the virtual IP, and the computers never know the difference."
