**Video Script 10.1: Datastream and Change Data Capture**
*Audio:* "Sometimes you don't want to move a database; you just want to copy its changes to a data warehouse in real-time. Datastream captures every `INSERT`, `UPDATE`, and `DELETE` from your Oracle or MySQL database and streams it directly into BigQuery for instant analytics without slowing down the primary database."
