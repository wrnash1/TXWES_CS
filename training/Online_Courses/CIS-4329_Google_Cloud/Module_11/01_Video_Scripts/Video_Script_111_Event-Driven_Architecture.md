**Video Script 11.1: Event-Driven Architecture**
*Audio:* "Cloud Functions are bits of code that run in response to an event. For example, when a user uploads an image to a Cloud Storage bucket, an event fires. A Cloud Function wakes up, creates a thumbnail of the image, and goes back to sleep. Pub/Sub acts as the messaging glue between these decoupled services."
